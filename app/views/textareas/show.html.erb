<br>
<%=text_area_tag(
	param_from_tablename(@mega_model_properties, displayable_field[:field].tablename) + "[" + displayable_field[:field].field + "]", 
	value
# , 	rows: displayable_field[:data_format].size 
	) if displayable_field[:data_format] %>
